<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Main Page</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container mt-5">
        <h1 class="text-center">Main Page</h1>
        <% for (let post of instaPost) { %>
            <div class="card mb-3">
                <div class="card-body">
                    <h5 class="card-title">@<%= post.username %></h5>
                    <h6 class="card-subtitle mb-2 text-muted">ID: <%= post.id %></h6>
                    <p class="card-text"><%= post.caption %></p>
                    <div class="d-flex justify-content-between">
                        <a class="btn btn-info" href="/instaPosts/<%= post.id %>">See in Detail</a>
                        <a class="btn btn-warning" href="/instaPosts/<%= post.id %>/edit">Edit</a>
                        <form action="/instaPosts/<%= post.id %>?_method=DELETE" method="post">
                            <button class="btn btn-danger" type="submit">Delete</button>
                        </form>
                    </div>
                </div>
            </div>
        <% } %>
        <div class="text-center">
            <a class="btn btn-primary" href="/instaPosts/new">Create New Post</a>
        </div>
    </div>
</body>
</html>
